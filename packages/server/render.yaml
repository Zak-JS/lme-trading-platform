services:
  - type: web
    name: lme-trading-server
    runtime: node
    rootDir: packages/server
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
