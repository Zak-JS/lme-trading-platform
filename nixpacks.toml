[phases.setup]
nixPkgs = ["nodejs_20", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm --filter @lme/shared build", "pnpm --filter @lme/server build"]

[start]
cmd = "cd packages/server && node dist/index.js"
